/*
  stringText = "currentState: ";
  stringMsg = stringText + currentState;
  Serial.println(stringMsg);
  
  unsigned long        previousMillis             = 0;
*/

/*
  if(decreaseIndex(indexSecond,9)){
    if(decreaseIndex(indexTenthOfASecond,5)){
      if(decreaseIndex(indexMinute,9){
        if(decreaseIndex(indexTenthOfASecond,9){
        }
      }
    }
  }
}


bool decreaseIndex(int index, int maxValue) {
  if (index != 0) {
    index--;
    return false;
  }
  
  index = maxValue;
  return true;
}
*/


/*
    case 5: //work routine
      ledsIndicatorsControl(LOW,LOW,LOW,LOW,HIGH,LOW);
      break;
    case 6: //rest routine
      ledsIndicatorsControl(LOW,LOW,LOW,LOW,LOW,HIGH);
      break;
    case 7: //free routine + work routine
      ledsIndicatorsControl(HIGH,LOW,LOW,LOW,HIGH,LOW);
      break;
    case 8: //free routine + rest routine
      ledsIndicatorsControl(HIGH,LOW,LOW,LOW,LOW,HIGH);
      break;
*/

/*
void assignDigitsForCountdown() {

  if (workRoutine) {
    indexSecond         = indexSecondOfWorkRoutine;
    indexTenthOfASecond = indexTenthOfASecondOfWorkRoutine;
    indexMinute         = indexMinuteOfWorkRoutine;
    indexTenthOfAMinute = indexTenthOfAMinuteOfWorkRoutine;
    
    return;
  }
    indexSecond         = indexSecondOfRestRoutine;
    indexTenthOfASecond = indexTenthOfASecondOfRestRoutine;
    indexMinute         = indexMinuteOfRestRoutine;
    indexTenthOfAMinute = indexTenthOfAMinuteOfRestRoutine;
}
*/